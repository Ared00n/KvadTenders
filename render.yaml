services:
  - type: web
    name: kvadrat-website
    env: go
    region: frankfurt
    plan: free
    buildCommand: go build -o main .
    startCommand: ./main
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /health